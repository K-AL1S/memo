# 项目说明

## 项目概览

这是一个极简静态网页任务管理项目，应用名和页面标识为 `NO FORGET`。项目用于记录待办事项、标记紧急程度、调整完成进度，并把完成一段时间后的事项归档到「已完成」页面。

项目没有构建系统、包管理配置或后端代码，当前全部功能由原生 HTML、CSS 和 JavaScript 实现。数据保存在浏览器 `localStorage` 中。

## 当前文件结构

- `memo.html`：主任务页面，展示已记录事项，并提供新增事项表单。
- `script.js`：主任务页面脚本，负责读取、保存、渲染任务，处理新增任务、紧急状态、完成进度和自动归档。
- `completed.html`：已完成页面，展示已归档的完成事项。
- `completed.js`：已完成页面脚本，负责读取并渲染归档完成项。
- `styles.css`：共享样式，负责纸张式布局、任务卡片、表单、进度控件、响应式适配和打印样式。
- `.gitignore`：忽略系统文件、编辑器目录、日志、临时文件、依赖目录、构建产物和环境变量文件。
- `AGENTS.md`：本文件，记录项目上下文和后续协作约定。

## 页面和功能

### `memo.html`

主页面标题为「我的任务」，主要区域包括：

- 页眉：显示 `NO FORGET`、页面标题、提示文案，并提供到 `completed.html` 的「已完成」导航链接。
- 已记录事项：通过 `<template id="task-template">` 渲染任务卡片，显示标题、详情、紧急复选框、完成进度、截止时间和剩余天数。
- 新增事项：表单包含标题、具体信息、截止日期、截止时间和紧急复选框。

### `completed.html`

已完成页面标题为「已完成」，主要区域包括：

- 页眉：显示 `NO FORGET`、页面标题、提示文案，并提供返回 `memo.html` 的导航链接。
- 归档完成项：通过 `<template id="completed-template">` 渲染已归档任务，显示标题、详情、完成时间、归档时间和原截止时间。

## 数据模型和业务逻辑

任务数据保存在浏览器 `localStorage`，键名为：

```text
no-forget-tasks
```

单个任务对象的主要字段：

- `id`：任务唯一标识，优先使用 `crypto.randomUUID()` 生成。
- `title`：任务标题。
- `detail`：任务具体信息。
- `deadline`：截止日期时间，格式类似 `YYYY-MM-DDTHH:mm`。
- `progress`：完成进度，数字范围为 `0` 到 `100`，步进为 `5`。
- `urgent`：是否紧急。
- `updatedAt`：最近更新时间，ISO 字符串。
- `completedAt`：首次达到 `100%` 时记录的完成时间。
- `archivedAt`：归档时间；有值时该任务进入已完成页面。

主页面逻辑要点：

- 新增任务会插入到任务数组开头。
- 默认截止时间设置为明天 `18:00`。
- 可在任务卡片中修改紧急状态和完成进度。
- 任务进度达到 `100%` 时会设置 `completedAt`。
- 如果任务进度从 `100%` 降回未完成，会清空 `completedAt` 和 `archivedAt`。
- 已完成任务在 `updatedAt` 超过 24 小时后自动设置 `archivedAt`，随后从主页面隐藏，并显示在 `completed.html`。
- 主页面排序优先级大致为：未完成优先、紧急优先、截止时间更早优先、进度更高优先、最近更新优先。

已完成页面逻辑要点：

- 只展示存在 `archivedAt` 的任务。
- 按归档时间倒序排列。
- 页面只读取和展示数据，不提供删除或恢复操作。

## 样式和设计约定

- 整体视觉是白色纸张放在浅灰页面背景上的任务文档。
- 主容器类名是 `.memo-page`，最大宽度约 `920px`，桌面端有边框和轻微阴影。
- 字体栈优先使用中文友好的系统字体：`Microsoft YaHei`、`PingFang SC`，并回退到 `Segoe UI`、`Arial`、`sans-serif`。
- CSS 变量集中定义在 `:root`，包括背景、纸张、文字、辅助文字、边框、蓝色强调色、警告色、危险色和完成色。
- `.task-card` 使用两列布局：左侧内容，右侧元信息和进度；窄屏下改为单列。
- 紧急任务通过 `.is-urgent-task` 强调标题颜色。
- 完成任务通过 `.is-completed-task` 使用更轻的背景。
- 剩余天数标签 `.days-left` 会根据普通、临近/逾期、已完成状态切换样式。
- 页面支持移动端适配：在 `760px` 以下，页眉、任务卡片和表单行会改为更窄屏友好的单列排列。
- 页面支持打印：`@media print` 中设置 A4 页面、去除背景、边框和阴影，并隐藏按钮。

## 运行方式

无需安装依赖。可以直接打开主页面：

```text
memo.html
```

已完成页面：

```text
completed.html
```

如果直接双击打开页面，`localStorage` 数据通常会按文件来源保存。若需要更接近真实网站环境，可以在项目根目录启动任意静态文件服务器，但当前项目不强制要求。

## 编码注意事项

- 项目文件使用 UTF-8。
- 在 Windows PowerShell 中读取中文文件时，建议显式指定编码，例如：

```powershell
Get-Content -Raw -Encoding UTF8 memo.html
```

否则中文可能在终端输出中显示为乱码，但这不代表文件内容一定损坏。

## 后续开发建议

- 修改页面文案时，保持 `lang="zh-CN"` 和 UTF-8 编码。
- 继续保持 JavaScript 外链文件组织：主页面逻辑放在 `script.js`，已完成页面逻辑放在 `completed.js`。
- 如果新增页面，优先复用 `styles.css` 中的现有布局、按钮、任务卡片和空状态样式。
- 如果修改任务字段，需要同步更新 `script.js` 和 `completed.js` 中的 `normalizeTask()`，避免旧数据读取失败。
- 如果修改归档规则，重点检查 `archiveOldCompletedTasks()`、`getVisibleTasks()` 和 `completed.js` 的筛选逻辑。
- 当前没有测试框架，改动后至少应在浏览器中手动验证：新增任务、修改紧急状态、调整进度、完成任务展示、归档页面展示、空状态和移动端布局。
- 如果增加更多页面或资源，继续保持静态项目的简单结构，除非确实需要引入构建工具。
- 修改样式时优先复用现有 CSS 变量和已有类名风格，避免无关的大范围重构。
- 打印体验仍然被样式支持，调整布局后应检查 `@media print` 是否仍然可用。

## 文件操作限制

禁止批量删除文件或目录。

不要使用以下命令或等价操作：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

如果确实需要删除文件，只能一次删除一个明确路径的文件，例如：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并请求用户手动删除。
